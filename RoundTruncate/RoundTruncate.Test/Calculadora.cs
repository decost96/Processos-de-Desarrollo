using System;

namespace RoundTruncate.Test
{
    public class Calculadora
    {
        public double devolver(double numeroDecimal)
        {
            return Math.Round(numeroDecimal);
        }
    }
}